* Misc
Install homebrew
https://brew.sh/

Install dotfiles
https://github.com/wtfleming/dotfiles

Install Fira code font
https://github.com/tonsky/FiraCode/wiki/Installing

Install FiraGO font
https://github.com/bBoxType/FiraGO
https://bboxtype.com/typefaces/FiraGO/

#+begin_src
brew install tmux wget htop watchman coreutils p7zip tree ncdu ripgrep cmake imagemagick
#+end_src

#+begin_src
brew install terminal-notifier
#+end_src

lld is a linker from llvm for Rust that is faster, sccache is a compiler cache for rust
#+begin_src
brew install lld sccache
#+end_src


1. Open System Settings → Notifications
2. Scroll down and look for terminal-notifier in the app list
3. Make sure Allow Notifications is turned ON and set to Banners or Alerts


* Claude Code
/plugin marketplace add anthropics/claude-code
/plugin install frontend-design@claude-code-plugins


** ast-grep
https://github.com/ast-grep/claude-skill
brew install ast-grep

/plugin marketplace add ast-grep/claude-skill
/plugin install ast-grep

* emacs
** Verified build recipe (emacs 30.2, Apple Silicon)
This is the recipe that actually produced the emacs 30.2 in use. Confirmed against
~/bin/emacs-30.2/config.log, which shows the working build used a plain configure
line with *no* CFLAGS / CPPFLAGS / LDFLAGS / TREE_SITTER_* overrides at all.

The older notes further down record a long detour chasing tree-sitter flags that
turned out to be unnecessary. See "Known gotchas" below for what was really going on.

*** 1. Prerequisites
#+begin_src sh
# Build toolchain.
#   make          -> gmake. macOS ships GNU Make 3.81 (2006); emacs wants newer.
#   texinfo       -> makeinfo, needed to build the manuals.
#   gcc/libgccjit -> required by --with-native-compilation.
brew install gcc libgccjit make gnutls texinfo autoconf pkg-config

# tree-sitter. The version has to match what the emacs version expects:
#   emacs 30.2 -> tree-sitter 0.25.x
#   emacs 31+  -> will probably want something newer
# Prefer the unversioned formula while it is still on the right version: it is
# not keg-only, so pkg-config finds it with zero extra configuration.
brew install tree-sitter

# Only if mainline has moved on and you need to pin. This one IS keg-only, which
# is the source of all the pain in the older notes below -- see gotcha #1.
# brew install tree-sitter@0.25

# emacs 29 only: jansson, for --with-json. Not needed from emacs 30 on, which
# ships its own JSON parser.
# brew install jansson

# Runtime helpers for my config
brew install ispell    # flyspell needs a spell checker
brew install pandoc    # markdown-mode uses this to preview markdown
brew install graphviz  # org-roam-graph shells out to dot
#+end_src

Verify before going further. This must print a 0.25.x version with no
PKG_CONFIG_PATH set:
#+begin_src sh
pkg-config --modversion tree-sitter
#+end_src
If it says "not found in the pkg-config search path", stop and read gotcha #1.
Do *not* reach for TREE_SITTER_CFLAGS.

Note there is no homebrew package to install for --with-xwidgets: on macOS it uses
the system WebKit.framework. It has nothing to do with the =wxwidgets= formula.

*** 2. Get the source
Download a release tag tarball from https://github.com/emacs-mirror/emacs/tags
(upstream is git://git.savannah.gnu.org/emacs.git if you would rather clone).

Unpack it somewhere you are willing to keep long term -- if you go the "run from
the build tree" route in step 5, this directory is where the binary lives.
~/bin/emacs-<version>/ is what I have been doing.

*** 3. Configure
#+begin_src sh
./autogen.sh

# ./configure --help lists everything
./configure \
    --with-native-compilation \
    --with-tree-sitter \
    --without-compress-install \
    --disable-gc-mark-trace \
    --with-xwidgets
#+end_src

That is the whole thing. No environment variable overrides.

What the flags do:
- --with-native-compilation :: bare (= "yes") natively compiles the preloaded lisp
  and JIT-compiles the rest on first use. Passing =aot= instead compiles every lisp
  file in the tree ahead of time: much slower build, no first-use pause later.
- --with-tree-sitter :: needs pkg-config to find tree-sitter (see step 1).
- --without-compress-install :: do not gzip the installed .el files.
- --disable-gc-mark-trace :: about 5% better garbage collection performance. It is
  enabled by default because disabling it makes it harder for emacs developers to
  help track down GC-related bugs.
- --with-xwidgets :: embedded WebKit views, via the system WebKit.framework.
- emacs 29 also needed --with-json; emacs 30 and newer do not.

configure prints a feature summary at the end. Check these lines before building:
#+begin_src
  Does Emacs use -ltree-sitter?                           yes
  Does Emacs support Xwidgets?                            yes
  Does Emacs have native lisp compiler?                   yes
#+end_src
If tree-sitter says "no" here, fix it now -- the build will otherwise succeed
without it and you will not notice until much later.

*** 4. Build
#+begin_src sh
gmake -j"$(sysctl -n hw.ncpu)"
#+end_src

Use sysctl, not =nproc=. nproc is GNU coreutils and is not present on a stock macOS.

Verify: =src/emacs -Q= starts.

*** 5. Install -- pick one
a) App bundle:
#+begin_src sh
gmake install                      # builds nextstep/Emacs.app
cp -R nextstep/Emacs.app /Applications/
#+end_src

b) Run out of the build tree -- keep the source dir and put its src/ on PATH:
#+begin_src sh
# in .zshrc
path+=("$HOME/bin/emacs-30.2/src")
#+end_src

(b) is what ~/bin is currently set up for. Whichever you pick, remember to bump the
version in the path when you upgrade, and delete the old source trees -- they are
several hundred MB each and they pile up.

*** 6. Verify the result
#+begin_src sh
emacs -Q --batch --eval '(progn
  (princ (format "treesit:     %s\n" (treesit-available-p)))
  (princ (format "native-comp: %s\n" (native-comp-available-p)))
  (princ (format "xwidgets:    %s\n" (fboundp (quote make-xwidget))))
  (princ (format "features:    %s\n" system-configuration-features)))'
#+end_src

All three predicates should be =t=. Expected feature list for this recipe:
#+begin_src
ACL GNUTLS LCMS2 LIBXML2 MODULES NATIVE_COMP NOTIFY KQUEUE NS PDUMPER PNG
SQLITE3 THREADS TOOLKIT_SCROLL_BARS TREE_SITTER WEBP XWIDGETS ZLIB
#+end_src

system-configuration-features only reports what was *compiled in*; the predicates
report what actually works at runtime. Check both -- see gotcha #2.

First launch with my config also needs:
#+begin_src
M-x all-the-icons-install-fonts
M-x nerd-icons-install-fonts
#+end_src

*** Known gotchas
**** 1. keg-only tree-sitter (this is what the older notes were fighting)
tree-sitter@0.25 is a versioned formula, so homebrew does not symlink it into
/opt/homebrew and its headers are not on the default include path. configure's
tree-sitter probe then compiles a test program calling ts_parser_new with no
declaration in scope; clang 16+ treats an implicit function declaration as a hard
error; configure concludes tree-sitter is missing. The clang change is the
messenger, not the bug -- the real problem is the missing -I.

The fix is to let pkg-config find the keg, not to silence the compiler:
#+begin_src sh
export PKG_CONFIG_PATH="/opt/homebrew/opt/tree-sitter@0.25/lib/pkgconfig"

pkg-config --cflags --libs tree-sitter
#   -I/opt/homebrew/Cellar/tree-sitter@0.25/0.25.10/include
#   -L/opt/homebrew/Cellar/tree-sitter@0.25/0.25.10/lib -ltree-sitter
#+end_src
Then run the plain ./configure from step 3, unchanged.

Do *not* set TREE_SITTER_CFLAGS. Per ./configure --help it *overrides* pkg-config
rather than adding to it, so setting it to "-Wno-error=..." deletes the -I path
pkg-config would have supplied. That is exactly the trap in the older notes below:
each override knocked out another flag, which then had to be restored by hand as
CPPFLAGS, then LDFLAGS, then TREE_SITTER_LIBS. PKG_CONFIG_PATH avoids the whole
chain and does not hardcode Cellar paths that go stale on the next upgrade.

**** 2. brew upgrade gcc can break native compilation
emacs links against a specific libgccjit. After upgrading gcc, NATIVE_COMP will
still appear in system-configuration-features (it was compiled in) while
(native-comp-available-p) returns nil and you get libgccjit errors at runtime.
The fix is to rebuild emacs. This is why step 6 checks the predicates and not
just the feature list.

**** 3. Dead ends -- do not re-investigate these
- Building with homebrew gcc instead of clang: no. The macOS build is a Cocoa/NS
  build and is partly Objective-C, which homebrew's gcc does not support.
- CC=clang-16: no such binary. Apple's clang is not installed under versioned names.
- Downgrading to clang 15: not feasible with Apple's toolchain, and unnecessary --
  see gotcha #1 for the actual cause.
- brew install wxwidgets: unrelated to emacs. "xwidgets" is an emacs feature name;
  the string "wxwidgets" does not appear anywhere in emacs configure.ac.

** Building emacs 31 (speculative -- not yet released)
Emacs 31 is not out yet. As of <2026-07-30> the latest pretest is 31.0.91
(announced 2026-07-23; the first pretest 31.0.90 was 2026-06-05) and no release
date has been announced. Everything below was checked against the =emacs-31=
branch of the emacs-mirror repo and must be re-verified against the real release.

*** Short answer on tree-sitter: use the unversioned formula, do NOT pin
#+begin_src sh
brew install tree-sitter        # 0.26.11 as of this writing
#+end_src

This is the *opposite* of the emacs 30.2 advice above, and the reason is worth
knowing because it also explains why 30.2 needed a pin in the first place.

- emacs 30.2 :: src/treesit.c calls =ts_language_version=. tree-sitter 0.26
  *removed* that function. So emacs 30.2 cannot build against homebrew's current
  mainline tree-sitter, which is why it needs tree-sitter@0.25 -- and since that
  formula is keg-only, that pin is the root of the whole PKG_CONFIG_PATH mess in
  gotcha #1.
- emacs 31 :: adds a compatibility shim, =treesit_language_abi_version=, which
  uses the new =ts_language_abi_version= when built against 0.25+ and falls back
  to the old function otherwise. So it builds against 0.26 fine.

Consequence: on emacs 31 you can use the plain =tree-sitter= formula, which is
*not* keg-only, so pkg-config finds it with no PKG_CONFIG_PATH and gotcha #1
simply does not apply. Do not carry the @0.25 pin forward out of habit.

*** The configure floor is not the version you actually want
The emacs-31 branch configure.ac still requires =tree-sitter >= 0.20.2= --
unchanged from emacs 30.2.

(If you see a claim that emacs 31 raised the minimum to 0.20.10, it is wrong.
Verified: emacs-31 configure.ac:4061 and emacs 30.2 configure.ac:4074 both say
0.20.2.)

But that floor is not the useful number, because ABI 15 support is decided at
*compile time* by the headers you build against:

#+begin_src c
  /* src/treesit.c, emacs-31 branch */
  #if TREE_SITTER_LANGUAGE_VERSION >= 15
    return ts_language_abi_version (ts_lang);
  #else
    return ts_language_version (ts_lang);
  #endif
#+end_src

tree-sitter 0.25's api.h is where that constant became 15
(=TREE_SITTER_LANGUAGE_VERSION 15=, =TREE_SITTER_MIN_COMPATIBLE_LANGUAGE_VERSION 13=).
So:

- built against tree-sitter < 0.25 :: configure passes, build succeeds, and you
  silently get *no* ABI 15 support
- built against 0.25 or newer :: ABI 15 support

The real requirement is therefore tree-sitter >= 0.25, which is stricter than what
configure enforces, and nothing will warn you. It matters because grammars built
by a current tree-sitter CLI are ABI 15, and an emacs built against an older
library rejects them at load time with a version-mismatch error.

*** Build recipe delta from the 30.2 recipe above
Steps 2-6 are expected to be unchanged. Differences:

1. Prerequisites: =brew install tree-sitter= (unversioned, unpinned). Skip
   gotcha #1 -- verify with =pkg-config --modversion tree-sitter= and expect it to
   work with no PKG_CONFIG_PATH set.
2. Configure: the same flag set is expected to work, but ~--with-xwidgets~ and
   ~--disable-gc-mark-trace~ are unverified for 31. Diff =./configure --help=
   against the release before assuming.
3. Verify: additionally check the ABI, which is the new thing worth checking.
   #+begin_src elisp
     (treesit-library-abi-version)     ; expect 15 or higher
     (treesit-library-abi-version t)   ; oldest compatible, expect 13
   #+end_src
   The optional MIN-COMPATIBLE argument is new in 31; in 30.2 the function takes
   no arguments. The function itself is *not* new -- it is already in 30.2.

*** What is actually new in 31 for tree-sitter
From etc/NEWS on the emacs-31 branch:
- =treesit-enabled-modes= :: new user option. Set to t to enable all available
  tree-sitter modes, or to a list to pick. Drives =major-mode-remap-alist= from
  the =treesit-major-mode-remap-alist= that mode packages prepare.
- =treesit-auto-install-grammar= :: new user option, defaults to ='ask=. Installs
  a missing grammar when a tree-sitter mode is turned on. Note it is a user
  option, not a function, and grammar installation itself is not new --
  =treesit-install-language-grammar= has existed since emacs 29. What is new is
  doing it automatically on demand.
- =treesit-extra-load-path= :: now a customizable user option; its first entry is
  the default install directory for the auto-install above.
- =treesit-language-source-alist= :: now supports keywords, e.g.
  =(json "https://github.com/tree-sitter/tree-sitter-json" :commit "4d770d3")=
- treesit-x.el :: new file providing =define-treesit-generic-mode= and a set of
  simple generic modes such as =gitattributes-generic-ts-mode=.
- Query predicates =:equal=, =:match= and =:pred= are deprecated, since newer
  tree-sitter libraries want predicates to end in =?=. =:equal= becomes =:eq?=.

Not a feature, despite appearances: =treesit--build-grammar= is a private
internal helper (note the double dash), not something to configure or rely on.

*** Re-check when 31.1 actually ships
- [ ] configure.ac tree-sitter minimum -- still 0.20.2?
- [ ] Does the mainline homebrew tree-sitter still build cleanly against it?
- [ ] Does the 30.2 flag set still configure without complaint?
- [ ] =(treesit-library-abi-version)= on the built emacs -- 15 or higher?
- [ ] =(native-comp-available-p)= -- does libgccjit need anything new?
- [ ] Is tree-sitter@0.25 still needed for anything, or can it be uninstalled?

Reference:
- https://github.com/emacs-mirror/emacs/tree/emacs-31
- https://lists.gnu.org/archive/html/emacs-devel/2026-07/msg00270.html (31.0.91)

** Original build notes (kept for reference)
Build emacs on an ARM Mac from source

If emacs build complains about tree-sitter and implicit-function-declaration
it is because clang 16 made it an error https://www.redhat.com/en/blog/new-warnings-and-errors-clang-16
and tree-sitter is still using the old behavior

-Wno-error=implicit-function-declaration

tree-sitter@0.25 is keg-only, which means it was not symlinked into /opt/homebrew,
because this is an alternate version of another formula.

For compilers to find tree-sitter@0.25 you may need to set:
  export LDFLAGS="-L/opt/homebrew/opt/tree-sitter@0.25/lib"
  export CPPFLAGS="-I/opt/homebrew/opt/tree-sitter@0.25/include"

i'm not sure the best way to fix this but

$ ./configure CFLAGS="-Wno-error=implicit-function-declaration" --with-native-compilation --with-tree-sitter --without-compress-install --disable-gc-mark-trace --with-xwidgets
$ gmake

gets us mostly there, but still have errors

./configure TREE_SITTER_CFLAGS="-Wno-error=implicit-function-declaration" LDFLAGS="-L/opt/homebrew/opt/tree-sitter/lib" CFLAGS="-Wno-error=implicit-function-declaration" --with-native-compilation --with-tree-sitter --without-compress-install --disable-gc-mark-trace --with-xwidgets

Might be possible to fix by compiling with gcc instead of clang?

./configure TREE_SITTER_CFLAGS="-Wno-error=implicit-function-declaration" LDFLAGS="-L/opt/homebrew/opt/tree-sitter/lib" CFLAGS="-Wno-error=implicit-function-declaration" --with-native-compilation --with-tree-sitter --without-compress-install --disable-gc-mark-trace --with-xwidgets


./configure TREE_SITTER_CFLAGS="-Wno-error=implicit-function-declaration" LDFLAGS="-L/opt/homebrew/opt/tree-sitter/lib" --with-native-compilation --with-tree-sitter --without-compress-install --disable-gc-mark-trace --with-xwidgets


CPPFLAGS="-I/opt/homebrew/opt/tree-sitter/include"

./configure TREE_SITTER_CFLAGS="-Wno-error=implicit-function-declaration" LDFLAGS="-L/opt/homebrew/opt/tree-sitter/lib" CPPFLAGS="-I/opt/homebrew/opt/tree-sitter/include" --with-native-compilation --with-tree-sitter --without-compress-install --disable-gc-mark-trace --with-xwidgets

  TREE_SITTER_CFLAGS
              C compiler flags for TREE_SITTER, overriding pkg-config
  TREE_SITTER_LIBS
              linker flags for TREE_SITTER, overriding pkg-config


./configure TREE_SITTER_CFLAGS="-Wno-error=implicit-function-declaration" LDFLAGS="-L/opt/homebrew/opt/tree-sitter@0.25/lib" CPPFLAGS="-I/opt/homebrew/opt/tree-sitter@0.25/include" --with-native-compilation --with-tree-sitter --without-compress-install --disable-gc-mark-trace --with-xwidgets




TREE_SITTER_LIBS="-L/opt/homebrew/opt/tree-sitter@0.25/lib -ltree-sitter" \

./configure TREE_SITTER_LIBS="-L/opt/homebrew/opt/tree-sitter@0.25/lib -ltree-sitter" TREE_SITTER_CFLAGS="-Wno-error=implicit-function-declaration" LDFLAGS="-L/opt/homebrew/opt/tree-sitter@0.25/lib" CPPFLAGS="-I/opt/homebrew/opt/tree-sitter@0.25/include" --with-native-compilation --with-tree-sitter --without-compress-install --disable-gc-mark-trace --with-xwidgets

CC=clang-16

# THIS SEEMS TO WORK! But maybe we should try compiling with gcc instead of clang?
or see if we can revert clang to 15 till the library fixes it

./configure TREE_SITTER_LIBS="-L/opt/homebrew/opt/tree-sitter@0.25/lib -ltree-sitter" TREE_SITTER_CFLAGS="-Wno-error=implicit-function-declaration" LDFLAGS="-L/opt/homebrew/opt/tree-sitter@0.25/lib" CPPFLAGS="-I/opt/homebrew/opt/tree-sitter@0.25/include" --with-native-compilation --with-tree-sitter --without-compress-install --disable-gc-mark-trace --with-xwidgets
gmake


# TODO try building using all processors

gmake -j$(nproc)



# TODO try installing normal tree-sitter (not @0.25) and see if we can make it build



#+begin_src

# note: emacs 30.2 needs tree sitter 0.25 to build
# emacs 31 or later will probably need a more recent version
brew install tree-sitter@0.25

# Note: if building for emacs 29 need to brew install jansson, but not for 30 and newer
brew install gcc libgccjit make gnutls texinfo autoconf pkg-config wxwidgets

# Emacs flyspell needs a spell checking tool, install one on macOS
brew install ispell

# Emacs markdown-mode uses this to preview markdown
brew install pandoc
#+end_src

Download emacs src from https://github.com/emacs-mirror/emacs/tags

Move the entire emacs src directory to ~/bin and make sure that
something like
path+=("$HOME/bin/emacs-emacs-29.4/src")
is in .zshrc

#+begin_src
  ./autogen.sh
#+end_src


Note that emacs 29 requires --with-json but we do not from emacs 30 on

 '--disable-gc-mark-trace'. This disables the GC mark trace buffer for about 5%
 better garbage collection performance.
 Doing so may make it more difficult for Emacs developers to help finding
 GC-related bugs that you run into, which is why the mark trace buffer is
 enabled by default.

 

 #+begin_src
./autogen.sh

# To see configure options run ./configure --help
./configure --with-native-compilation --with-tree-sitter --without-compress-install --disable-gc-mark-trace --with-xwidgets

# Run 'make' to build Emacs, then run 'src/emacs' to test it.
Run 'gmake' to build Emacs, then run 'src/emacs' to test it.

# Run 'gmake install'

# For my config currently need to run these commands
M-x all-the-icons-install-fonts
M-x nerd-icons-install-fonts
#+end_src


You can see what features emacs was built with by looking at variable system-configuration-features


** LLMs with gptel
In ~/.authinfo have the following lines:

#+begin_src
machine api.openai.com login apikey password <api key goes here>
machine api.anthropic.com login apikey password <api key goes here>
#+end_src


* Misc homebrew
#+begin_src sh
  # Python
  brew install uv

  # Clojure things
  brew install borkdude/brew/clj-kondo
  brew install clojure-lsp/brew/clojure-lsp-native
  brew install borkdude/brew/babashka

  # Shell scripts linter
  brew install shellcheck

  # YAML language server
  brew install yaml-language-server

  # TypeScript language server
  # Claude Code's typescript-lsp plugin needs this binary on PATH and does not install it
  brew install typescript-language-server

  # Glow is a terminal based markdown reader
  brew install glow

  # Terraform language server
  brew install hashicorp/tap/terraform-ls

  # For erlang installed with mise or asdf
  brew install autoconf
  # For Erlang built with wxWidgets (start observer or debugger!)
  # Formerly the wxmac formula, which is now just an old name for this one.
  brew install wxwidgets

  # For exporting LaTex from emacs
  brew install mactex

#+end_src

* Git
Install dev tools - this is one way to prompt it:
#+begin_src
git --version

brew install git-lfs
git lfs install
git config --global core.excludesfile ~/.gitexcludes
#+end_src



* Mission Control
Disable mission control since I use those keyboard shortcuts in emacs


Go to System Settings > Keyboard > Keyboard Shortcuts > Mission Control and change the settings for "Move left a space" and "Move right a space" or disable them completely.
http://apple.stackexchange.com/questions/18043/how-can-i-make-ctrlright-left-arrow-stop-changing-desktops-in-lion

If a japanese keyboard is installed, need to disable Control-space since I use that in emacs a bunch
Go to System Settings > Keyboard > Keyboard Shortcuts > Input Sources
  Disable the 2 select an input source shortcuts

Go to System Settings > Keyboard > Dictation > Shortcut
  And change it to not be pressing control twice

* Java
** Current recipe
The old notes below are broken: they install =openjdk@21= but every path that
follows points at the *unversioned* =openjdk= keg. Those caveats came from
=brew info openjdk=, not =brew info openjdk@21=, and the two are different
prefixes. Following them symlinks a different JDK than the one you installed,
or nothing at all.

Symptom, and the state of this machine as of <2026-07-30>: java is not working.
=/Library/Java/JavaVirtualMachines/= is empty, =/usr/libexec/java_home= reports
"Unable to locate a Java Runtime", and =java -version= fails -- even though
openjdk plus @23, @24, @25 and @26 are all installed. They are keg-only, so
installing them registers nothing with macOS on its own.

*** Install
Pick the version deliberately and use it consistently. Substitute for NN below
(the machine currently has 23, 24, 25, 26; there is no 21 despite what the old
notes say):

#+begin_src sh
brew install openjdk@NN

# Everything after this must use the SAME @NN. Get the prefix from brew rather
# than typing it, so the two cannot drift apart:
JDK="$(brew --prefix openjdk@NN)"

# Register it with macOS so /usr/libexec/java_home and the system wrappers see it
sudo ln -sfn "$JDK/libexec/openjdk.jdk" /Library/Java/JavaVirtualMachines/openjdk-NN.jdk
#+end_src

Naming the symlink openjdk-NN.jdk rather than openjdk.jdk lets several JDKs be
registered at once and selected with =/usr/libexec/java_home -v NN=.

openjdk is keg-only -- homebrew does not symlink it into /opt/homebrew, because
macOS ships similar software and installing in parallel causes trouble. That is
why the registration step above is required and not optional.

*** PATH
The old notes say to =echo ... >> ~/.zshrc=. Do not: sync-dotfiles.sh copies
Dotfiles/zshrc over ~/.zshrc, so that gets wiped on the next sync. Put it in
=Dotfiles/zshrc= and re-sync.

Note Dotfiles/zshrc:49 currently reads
=export PATH="/usr/local/opt/openjdk/bin:$PATH"=. /usr/local is the *Intel*
homebrew prefix; on Apple Silicon it is /opt/homebrew, and /usr/local/opt/openjdk
does not exist here. The line is a harmless no-op rather than the cause of the
breakage above, but it has never done anything on this machine. Replace it with
the versioned path when you decide which JDK should be the default:

#+begin_src sh
export PATH="/opt/homebrew/opt/openjdk@NN/bin:$PATH"
#+end_src

Or leave PATH alone and select per-shell with =export JAVA_HOME="$(/usr/libexec/java_home -v NN)"=.

For compilers to find the headers:
#+begin_src sh
export CPPFLAGS="-I/opt/homebrew/opt/openjdk@NN/include"
#+end_src

*** Verify
#+begin_src sh
java -version                    # should print the version, not an error
/usr/libexec/java_home -V        # should list the JDK you registered
ls /Library/Java/JavaVirtualMachines/
#+end_src

An empty /Library/Java/JavaVirtualMachines means the =ln -sfn= step above did not
happen. That is the current state, and it is what "Unable to locate a Java
Runtime" is telling you -- the JDK is installed, macOS just cannot see it.

** Original notes (kept for reference -- paths do not match the install)

$ brew install openjdk@21
For the system Java wrappers to find this JDK, symlink it with
  sudo ln -sfn /opt/homebrew/opt/openjdk/libexec/openjdk.jdk /Library/Java/JavaVirtualMachines/openjdk.jdk

openjdk is keg-only, which means it was not symlinked into /opt/homebrew,
because macOS provides similar software and installing this software in
parallel can cause all kinds of trouble.

If you need to have openjdk first in your PATH, run:
  echo 'export PATH="/opt/homebrew/opt/openjdk/bin:$PATH"' >> ~/.zshrc

For compilers to find openjdk you may need to set:
  export CPPFLAGS="-I/opt/homebrew/opt/openjdk/include"

Verify installation
  java -version

** Install Clojure CLI tools
#+begin_src
$ brew install clojure/tools/clojure

$ clj
user=> (+ 1 2)
3

#+end_src

*** Install leiningen
#+begin_src
$ curl https://raw.githubusercontent.com/technomancy/leiningen/stable/bin/lein > ~/bin/lein
$ chmod a+x ~/bin/lein

# Verify installation
$ lein version

#+end_src


* Docker
https://docs.docker.com/desktop/mac/install/

$ docker login
Username is not the default email displayed, look it up by logging into https://hub.docker.com

* awscli
brew install awscli
$ aws configure


* mise
** Current recipe
Unlike the emacs section, this one is *not* verified against a working setup --
mise is not installed on this machine as of <2026-07-30>. Checked against upstream
docs only.

*** Install and activate
#+begin_src sh
brew install mise
#+end_src

The activation line goes in =Dotfiles/zshrc= in this repo, *not* in ~/.zshrc:

#+begin_src sh
eval "$(mise activate zsh)"
#+end_src

then re-run ./sync-dotfiles.sh and open a new shell.

This is the important correction to the old notes below, which say
=echo 'eval "$(mise activate zsh)"' >> ~/.zshrc=. sync-dotfiles.sh does
=cp ~/src/dotfiles/Dotfiles/zshrc ~/.zshrc=, so anything appended to ~/.zshrc
directly is silently wiped the next time the dotfiles are synced.

*** Install tools -- mind the -g
#+begin_src sh
# GLOBAL: writes ~/.config/mise/config.toml. This is what you want for machine setup.
mise use -g erlang@28.4.1
mise use -g elixir@1.19.5

# or track latest
mise use -g erlang@latest
mise use -g elixir@latest

# PER-PROJECT: writes ./mise.toml in the current directory
cd ~/src/someproject && mise use elixir@1.19.5
#+end_src

=mise use= without =-g= writes a mise.toml *in the current directory*. The old
notes below omit the flag, so following them from the home directory leaves a
stray ~/mise.toml and no global default.

*** Verify
#+begin_src sh
mise doctor        # run after activation, not before -- it checks the shell hookup
mise ls            # installed tools, and which config file each came from
which erl elixir
#+end_src

*** Erlang-specific environment
Already set in Dotfiles/zshrc, and it applies to mise too despite the comment
there saying asdf:
- =KERL_BUILD_DOCS="yes"= :: builds the Erlang docs so iex =h= works on Erlang
  modules, e.g. =h :unicode.bom_to_encoding=. mise builds Erlang with kerl, same
  as the asdf plugin did, so the variable is read either way.
- =ERL_AFLAGS= :: shell history in iex and erl.

For observer/debugger you need wxWidgets:
#+begin_src sh
brew install wxwidgets
# iex> :observer.start()
#+end_src
This is the *real* use for the wxwidgets formula, and probably where the emacs
notes picked it up from -- it does nothing for emacs, see emacs gotcha #3.

** Original notes (kept for reference)
#+begin_src
brew install mise

mise doctor
  
mise use erlang@28.4.1
mise use elixir@1.19.5

or
  
mise use erlang@latest
mise use elixir@latest

echo 'eval "$(mise activate zsh)"' >> ~/.zshrc
source ~/.zshrc
  
#+end_src
* asdf
** Current recipe (asdf 0.16+, the Go rewrite)
Also not verified on this machine: asdf is not installed, and the activation line
in Dotfiles/zshrc:52 is commented out. From upstream docs.

asdf was rewritten in Go in v0.16.0 (2025-01-30). The notes below pin
=--branch v0.14.0=, the old shell implementation, and every shell-integration
instruction in them is obsolete. If you have moved to mise (see that section)
you probably do not need asdf at all -- this is here for projects that require it.

*** What changed in 0.16
| old (0.14, shell)         | new (0.16+, Go binary)     |
|---------------------------+----------------------------|
| . "$HOME/.asdf/asdf.sh"   | removed; put shims on PATH |
| asdf global <tool> <ver>  | asdf set --home <tool> <ver> |
| asdf local <tool> <ver>   | asdf set <tool> <ver>      |
| asdf plugin-add           | asdf plugin add            |
| git clone a tagged branch | brew install asdf          |

All hyphenated subcommands lost their hyphen, not just plugin-add.

*** Install
#+begin_src sh
brew install asdf
#+end_src

The old =brew install coreutils curl git= line was for the shell implementation.
The Go binary does not need coreutils.

*** Shell setup
In =Dotfiles/zshrc= -- not ~/.zshrc, which sync-dotfiles.sh overwrites -- replace
the commented-out asdf.sh line with:

#+begin_src sh
export PATH="${ASDF_DATA_DIR:-$HOME/.asdf}/shims:$PATH"
#+end_src

ASDF_DATA_DIR defaults to $HOME/.asdf, so setting it explicitly is optional; the
shims directory on PATH is the part that matters. Confirm with =asdf info=.

The completions advice below references =$ASDF_DIR/completions=. ASDF_DIR belonged
to the shell implementation. Use =asdf completion zsh= instead, but check
=asdf completion --help= against the installed version before wiring it into zshrc
-- I have not verified the exact invocation.

The old notes also warn that compinit must come *below* the asdf sourcing. In
Dotfiles/zshrc compinit is at line 44 and the asdf line at 52, i.e. already the
opposite order, so that advice was being violated anyway. Moot under 0.16, which
has no asdf.sh to source, but relevant if you ever go back to 0.14.

*** Erlang/Elixir under 0.16
The Erlang/Elixir subsection below still uses 0.14 syntax. Translated, with
versions bumped to match the mise section:
#+begin_src sh
asdf plugin add erlang
asdf plugin add elixir

asdf list all erlang
asdf list all elixir

asdf install erlang 28.4.1
asdf set --home erlang 28.4.1
asdf install elixir 1.19.5-otp-28
asdf set --home elixir 1.19.5-otp-28

mix local.rebar --if-missing --force
mix local.hex --if-missing --force
mix archive.install hex phx_new
#+end_src

Elixir builds from the asdf plugin are suffixed with the OTP major they were
compiled against, so the =-otp-NN= has to track the erlang version. Run
=asdf list all elixir= for the suffix that actually exists -- I have not confirmed
1.19.5-otp-28 is published.

The wxwidgets note in the old subsection is correct and still applies: Erlang's
observer and debugger are wxWidgets apps.

Reference: https://asdf-vm.com/guide/upgrading-to-v0-16.html

** Original notes (kept for reference -- asdf 0.14, superseded)
See https://asdf-vm.com/guide/getting-started.html

brew install coreutils curl git

git clone https://github.com/asdf-vm/asdf.git ~/.asdf --branch v0.14.0

Add the following to ~/.zshrc:

. "$HOME/.asdf/asdf.sh"

Completions are configured by either a ZSH Framework asdf plugin or by adding the following to your .zshrc:

#+begin_src
# append completions to fpath
fpath=(${ASDF_DIR}/completions $fpath)
# initialise completions with ZSH's compinit
autoload -Uz compinit && compinit

#+end_src

if you are using a custom compinit setup, ensure compinit is below your sourcing of asdf.sh
if you are using a custom compinit setup with a ZSH Framework, ensure compinit is below your sourcing of the framework

** Erlang/Elixir
#+begin_src
asdf plugin-add erlang
asdf plugin-add elixir

asdf list all elixir
asdf list all erlang


asdf install erlang 25.3.2
asdf global erlang 25.3.2
asdf install elixir 1.14.4-otp-25
asdf global elixir 1.14.4-otp-25


For building with wxWidgets (start observer or debugger!)
brew install wxwidgets

You can test that it worked with
iex> :observer.start()

mix local.rebar --if-missing --force
mix local.hex --if-missing --force

mix archive.install hex phx_new

#+end_src

* Japanese keyboard
System Settings->Keyboard->Text input
  Add Japanese - Romaji source
  Enable "Use Caps Lock key to switch to and from U.S."

* Rust
https://rustup.rs/
https://github.com/rust-lang/rust-analyzer
#+begin_src
$ brew install rust-analyzer
#+end_src


* Tailwind CSS CLI tool
#+begin_src
curl -sLO https://github.com/tailwindlabs/tailwindcss/releases/latest/download/tailwindcss-macos-arm64
chmod +x tailwindcss-macos-arm64
mv tailwindcss-macos-arm64 tailwindcss
#+end_src

* aider
brew install aider

Add api keys to .env see https://aider.chat/docs/config/dotenv.html
